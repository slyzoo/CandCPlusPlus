#include <windows.h>
#include <stdio.h>

// This wont ever be used in a serious manner, its just something i thought was fun

int main(){

  int hertz = 180;
  int duration = 1000; // in ms

  Beep(hertz, duration);

return 0;
}